name: Project Ci

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.12"]

    steps:

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r /Python/Project/src/requirements.txt

    - name: Run tests
      run: |
        python -m unittest discover -s /Python/Project/src/requirements.txt -p "*.py"

